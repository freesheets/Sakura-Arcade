# Custom Hooks

Esta pasta contém hooks customizados do React.

## Exemplo de uso

```javascript
import { useExample } from './hooks/useExample'

function MyComponent() {
  const { data, loading, error } = useExample()
  // ...
}
```

